/**
 * Compras server actions — Módulo 4 (Compras). Dueño: Compras.
 *
 * FASE 0: shell estructural, sin lógica de negocio.
 *
 * Mapa de exposición:
 * - `createPurchase` (Server Action, SOLO admin — cashier lee /compras) →
 *   Vitest integración PostgreSQL
 * - búsqueda modal + historial → GET /api/compras/search, /api/compras →
 *   API tests
 *
 * Implementación: design del módulo Compras (HU-4.x).
 */