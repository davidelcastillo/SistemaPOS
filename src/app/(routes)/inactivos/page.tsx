/**
 * Inactivos page placeholder — FASE 0.
 *
 * Module 2 (Inventario), admin-only route: lists soft-deleted catalog items.
 */
export default function InactivosPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <h1 className="text-2xl font-semibold">Inactivos</h1>
    </main>
  );
}