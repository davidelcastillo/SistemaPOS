/**
 * Compras page placeholder — FASE 0.
 *
 * Module 4 (Compras): cashier access is read-only; creation is admin-only
 * (server-side role validation). Implementation lands in its own design.
 */
export default function ComprasPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <h1 className="text-2xl font-semibold">Compras</h1>
    </main>
  );
}